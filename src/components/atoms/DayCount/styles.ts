import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  dayCount: {
    alignItems: 'center',
    paddingHorizontal: 4
  },
  dayCountNumber: {
    color: '#29C13E',
    fontWeight: '600',
    fontSize: 18,
  },
  dayCountText: {
    color: '#959595',
    fontSize: 10,
    lineHeight: 10
  }
});

export default styles;