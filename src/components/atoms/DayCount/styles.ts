import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  dayCount: {
    alignItems: 'center',
  },
  dayCountNumber: {
    color: '#29C13E',
    fontWeight: '600',
    fontSize: 18,
  },
  dayCountText: {
    color: '#959595',
    fontSize: 12
  }
});

export default styles;