import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headingContainer: {
    width: '100%',
    marginBottom: 32
  },
  subHeading: {
    color: '#959595',
    fontSize: 14,
    fontWeight: '500',
    marginBottom: 4
  },
  heading: {
    color: '#FDFDFD',
    fontWeight: '700',
    fontSize: 28,
  }
});

export default styles;